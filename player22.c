#include<stdio.h>
int main(void)
  {
      int a,b,c,i;
      scanf("%d %d", &a,&b);
      for(i=2;i<=n;i++)
      {
          if(b%i==0 && a%i==0)
          {
              c=i;
          }
      }
      printf("%d"c);
      return 0;
  }
